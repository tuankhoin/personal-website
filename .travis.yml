language: node_js
before_script:
  - npm install -g gatsby-cli
node_js:
  - "10"
deploy:
  provider: script
  # Note: change "docs" to the directory where your gatsby-site lives, if necessary
  script: echo "skip test"# cd C:\Users\HP OMEN 15\Desktop\Workspace\hello-world && yarn install && yarn run deploy
  skip_cleanup: true
  on:
    branch: master